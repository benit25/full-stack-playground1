{
  "name": "full-stack-plxyground-root",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install --include=dev && npm --prefix frontend run build && npm --prefix admin-panel install --include=dev && npm --prefix admin-panel run build"
  }
}
